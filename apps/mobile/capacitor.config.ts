import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.wealthgate.lockedscreen.mobile",
  appName: "Lockedscreen Mobile",
  webDir: "dist"
};

export default config;
