<Window x:Class="Lockedscreen.App.Views.UnlockPinDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Enter Unlock PIN" Height="220" Width="420" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Invigilator PIN required to exit." FontWeight="SemiBold" />
        <PasswordBox x:Name="PinBox" Grid.Row="1" Margin="0,12,0,0" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" Click="Cancel_Click" Width="80" Margin="0,0,8,0" />
            <Button Content="Unlock" Click="Unlock_Click" Width="80" />
        </StackPanel>
    </Grid>
</Window>
