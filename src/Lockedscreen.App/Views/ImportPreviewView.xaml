<UserControl x:Class="Lockedscreen.App.Views.ImportPreviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:Lockedscreen.App.Controls">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <TextBlock Text="Import Preview" Style="{DynamicResource SectionHeader}" />
            <Button Content="Back" Command="{Binding BackCommand}" Style="{DynamicResource GhostButton}" Margin="16,4,0,0" />
        </StackPanel>

        <ItemsControl ItemsSource="{Binding Warnings}" Grid.Row="1">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" Foreground="{DynamicResource DangerBrush}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Border Grid.Row="2" Background="{DynamicResource SurfaceBrush}" Padding="12">
            <controls:HtmlPreviewControl Html="{Binding SourceHtml}" />
        </Border>
    </Grid>
</UserControl>
