<UserControl x:Class="Lockedscreen.App.Views.CreateExamView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24">
            <TextBlock Text="Create Exam" Style="{DynamicResource SectionHeader}" />

            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                <StackPanel Width="420" Margin="0,0,24,0">
                    <TextBlock Text="Exam Name" />
                    <TextBox Text="{Binding Draft.Name}" />
                </StackPanel>
                <StackPanel Width="140">
                    <TextBlock Text="Duration (min)" />
                    <TextBox Text="{Binding Draft.DurationMinutes}" />
                </StackPanel>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                <StackPanel Width="300" Margin="0,0,24,0">
                    <TextBlock Text="Start Time (Local)" />
                    <DatePicker SelectedDate="{Binding Draft.StartTimeLocal}" />
                </StackPanel>
                <CheckBox Content="Require Student ID" IsChecked="{Binding Draft.RequireStudentId}" VerticalAlignment="Bottom" />
            </StackPanel>

            <TextBlock Text="Instructions (HTML)" />
            <TextBox Text="{Binding Draft.InstructionsHtml}" AcceptsReturn="True" Height="120" />

            <TextBlock Text="Display Settings" Margin="0,16,0,8" />
            <StackPanel Orientation="Horizontal">
                <StackPanel Width="200" Margin="0,0,16,0">
                    <TextBlock Text="Theme" />
                    <ComboBox SelectedIndex="{Binding Draft.Settings.Theme}" SelectionChanged="ThemeSelectionChanged">
                        <ComboBoxItem Content="System" />
                        <ComboBoxItem Content="Light" />
                        <ComboBoxItem Content="Dark" />
                    </ComboBox>
                </StackPanel>
                <StackPanel Width="200" Margin="0,0,16,0">
                    <TextBlock Text="Font Scale" />
                    <TextBox Text="{Binding Draft.Settings.FontScale}" />
                </StackPanel>
                <StackPanel Width="200">
                    <TextBlock Text="Layout Density" />
                    <ComboBox SelectedIndex="{Binding Draft.Settings.Density}" SelectionChanged="DensitySelectionChanged">
                        <ComboBoxItem Content="Comfortable" />
                        <ComboBoxItem Content="Compact" />
                    </ComboBox>
                </StackPanel>
            </StackPanel>

            <TextBlock Text="Source" Margin="0,16,0,8" />
            <StackPanel Orientation="Horizontal">
                <TextBox Text="{Binding Draft.SourcePath}" Width="360" ToolTip="Path to DOCX or HTML" />
                <Button Content="Import DOCX" Command="{Binding ImportDocxCommand}" Style="{DynamicResource GhostButton}" Margin="8,0,0,0" />
                <Button Content="Import HTML" Command="{Binding ImportHtmlCommand}" Style="{DynamicResource GhostButton}" Margin="8,0,0,0" />
                <Button Content="Preview" Command="{Binding PreviewImportCommand}" Style="{DynamicResource GhostButton}" Margin="8,0,0,0" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                <TextBox Text="{Binding Draft.SourceUrl}" Width="360" ToolTip="Hosted exam URL" />
                <Button Content="Use URL" Command="{Binding SetUrlSourceCommand}" Style="{DynamicResource GhostButton}" Margin="8,0,0,0" />
            </StackPanel>

            <ItemsControl ItemsSource="{Binding ImportWarnings}" Margin="0,8,0,0">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Foreground="{DynamicResource DangerBrush}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock Text="Questions" Margin="0,16,0,8" />
            <Button Content="Add MCQ" Command="{Binding AddQuestionCommand}" Style="{DynamicResource PrimaryButton}" Width="120" />
            <ItemsControl ItemsSource="{Binding Draft.Questions}" Margin="0,8,0,0">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{DynamicResource SurfaceBrush}" Padding="12" Margin="0,0,0,8">
                            <StackPanel>
                                <TextBlock Text="{Binding PromptHtml}" />
                                <ItemsControl ItemsSource="{Binding Choices}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding TextHtml}" Foreground="{DynamicResource MutedTextBrush}" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <Button Content="Remove" Command="{Binding DataContext.RemoveQuestionCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}" Style="{DynamicResource GhostButton}" Width="90" Margin="0,8,0,0" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock Text="{Binding StatusMessage}" Foreground="{DynamicResource MutedTextBrush}" Margin="0,8,0,0" />

            <Button Content="Next: Answer Key" Command="{Binding NextToAnswerKeyCommand}" Style="{DynamicResource PrimaryButton}" Width="180" Margin="0,16,0,0" />
        </StackPanel>
    </ScrollViewer>
</UserControl>
