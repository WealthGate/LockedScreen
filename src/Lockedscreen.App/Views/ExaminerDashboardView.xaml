<UserControl x:Class="Lockedscreen.App.Views.ExaminerDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid Margin="24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="0,0,24,0">
            <TextBlock Text="Examiner Dashboard" Style="{DynamicResource SectionHeader}" />
            <TextBlock Text="Create or edit exams. Import documents or configure hosted links." Foreground="{DynamicResource MutedTextBrush}" />

            <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                <Button Content="Refresh" Command="{Binding RefreshCommand}" Style="{DynamicResource GhostButton}" />
                <Button Content="Create New" Command="{Binding CreateNewCommand}" Style="{DynamicResource PrimaryButton}" Margin="8,0,0,0" />
            </StackPanel>

            <TextBlock Text="Existing Exams" Margin="0,24,0,8" />
            <ListBox ItemsSource="{Binding Exams}" SelectedItem="{Binding SelectedExam}" Height="320">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="4">
                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding CreatedUtc}" Foreground="{DynamicResource MutedTextBrush}" FontSize="12" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Button Content="Load Selected" Command="{Binding LoadSelectedCommand}" Style="{DynamicResource PrimaryButton}" Margin="0,12,0,0" />
        </StackPanel>

        <Border Grid.Column="1" Background="{DynamicResource SurfaceBrush}" Padding="24">
            <StackPanel>
                <TextBlock Text="Quick Checklist" Style="{DynamicResource SectionHeader}" />
                <TextBlock Text="1. Import DOCX/HTML or paste URL" />
                <TextBlock Text="2. Add or edit MCQ questions" />
                <TextBlock Text="3. Set answer key and duration" />
                <TextBlock Text="4. Publish exam package" />
                <TextBlock Text="5. Enable kiosk with helper" Margin="0,12,0,0" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
