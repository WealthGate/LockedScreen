<UserControl x:Class="Lockedscreen.App.Views.StudentExamView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             xmlns:controls="clr-namespace:Lockedscreen.App.Controls">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,12">
            <StackPanel DockPanel.Dock="Left">
                <TextBlock Text="Exam In Progress" Style="{DynamicResource SectionHeader}" />
                <TextBlock Text="{Binding StudentDisplay}" Foreground="{DynamicResource MutedTextBrush}" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="Time Remaining:" Margin="0,0,8,0" />
                <TextBlock Text="{Binding TimeRemainingText}" FontWeight="SemiBold" />
                <Button Content="Review" Command="{Binding ReviewCommand}" Style="{DynamicResource GhostButton}" Margin="16,0,0,0" />
                <Button Content="Submit" Command="{Binding SubmitCommand}" Style="{DynamicResource PrimaryButton}" Margin="8,0,0,0" />
            </StackPanel>
        </DockPanel>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border Background="{DynamicResource SurfaceBrush}" Padding="16" Visibility="{Binding IsUrlExam, Converter={DynamicResource BoolToVis}}">
                <wv2:WebView2 x:Name="ExamWebView" />
            </Border>

            <Grid Visibility="{Binding IsUrlExam, Converter={DynamicResource InverseBoolToVis}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Border Background="{DynamicResource SurfaceBrush}" Padding="16" Margin="0,0,0,12">
                    <controls:HtmlPreviewControl Html="{Binding CurrentQuestion.PromptHtml}" />
                </Border>
                <ItemsControl Grid.Row="1" ItemsSource="{Binding CurrentChoices}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Content="{Binding TextHtml}" Command="{Binding DataContext.SelectChoiceCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{DynamicResource GhostButton}" Margin="0,0,0,8" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>

            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                <Button Content="Prev" Command="{Binding PrevCommand}" Style="{DynamicResource GhostButton}" />
                <Button Content="Next" Command="{Binding NextCommand}" Style="{DynamicResource GhostButton}" Margin="8,0,0,0" />
                <Button Content="Flag" Command="{Binding FlagCommand}" Style="{DynamicResource GhostButton}" Margin="8,0,0,0" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
