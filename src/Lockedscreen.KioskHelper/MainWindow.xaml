<Window x:Class="Lockedscreen.KioskHelper.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Lockedscreen Kiosk Helper" Height="720" Width="980" Background="#F4F5F7">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Kiosk Setup Helper" FontSize="24" FontWeight="SemiBold" />

        <StackPanel Grid.Row="1" Margin="0,16,0,0">
            <TextBlock Text="Exam User" />
            <StackPanel Orientation="Horizontal">
                <TextBox x:Name="UserNameBox" Width="180" Text="ExamUser" />
                <PasswordBox x:Name="PasswordBox" Width="180" Margin="8,0,0,0" />
                <Button Content="Create User" Click="CreateUser_Click" Margin="8,0,0,0" />
            </StackPanel>

            <TextBlock Text="Lockedscreen App Path" Margin="0,12,0,0" />
            <StackPanel Orientation="Horizontal">
                <TextBox x:Name="AppPathBox" Width="520" Text="C:\\Program Files\\Lockedscreen\\Lockedscreen.App.exe" />
                <Button Content="Enable Kiosk" Click="EnableKiosk_Click" Margin="8,0,0,0" />
                <Button Content="Disable Kiosk" Click="DisableKiosk_Click" Margin="8,0,0,0" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                <Button Content="Open Assigned Access Settings" Click="OpenSettings_Click" />
                <Button Content="Check WebView2 Runtime" Click="CheckWebView_Click" Margin="8,0,0,0" />
            </StackPanel>
        </StackPanel>

        <TextBox Grid.Row="2" x:Name="OutputBox" Margin="0,16,0,0" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" />
    </Grid>
</Window>
